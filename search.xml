<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>博客书写规范</title>
    <url>/2019/03/26/hello-world/</url>
    <content><![CDATA[<h3 id="md文件注意事项"><a href="#md文件注意事项" class="headerlink" title="md文件注意事项"></a>md文件注意事项</h3><h4 id="标签和分类设置"><a href="#标签和分类设置" class="headerlink" title="标签和分类设置"></a>标签和分类设置</h4><p>当要为某一篇文章添加标签，只需在<code>myBlog/source/_post</code>目录下的具体文章的tags中添加标签即可，如：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">---</span><br><span class="line">title: 基于Hexo和Github搭建博客</span><br><span class="line">date: 2019-03-26</span><br><span class="line">tags: [npm, hexo, github]</span><br><span class="line">categories: 搭建博客</span><br><span class="line">---</span><br></pre></td></tr></table></figure>
<ul>
<li>设置具体文章的categories</li>
</ul>
<p>当要为某一篇文章添加分类，只需在<code>myBlog/source/_post</code>目录下的具体文章的categories中添加分类即可，如：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">---</span><br><span class="line">title: 基于Hexo和Github搭建博客</span><br><span class="line">date: 2016-11-09</span><br><span class="line">tags: [npm, hexo, github]</span><br><span class="line">categories: 搭建博客</span><br><span class="line">---</span><br></pre></td></tr></table></figure>
<h4 id="图片设置"><a href="#图片设置" class="headerlink" title="图片设置"></a>图片设置</h4><p>图片素材按官方教程说法，可统一放置在source/images目录中，并以 <code>![](/images/image.jpg)</code> 方式引用</p>
<p>或者在 _config.yml 打开 post_asset_folder 功能，将当前文章所用的图片放置到source目录下的文章同名资源目录下，以 <code>![](image.jpg)</code> 方式引用</p>
<p>另外一些文章中提到建议使用第三方的专业图床来存放这些静态图片素材 🧐</p>
<h3 id="hexo-s-–debug"><a href="#hexo-s-–debug" class="headerlink" title="hexo s –debug"></a>hexo s –debug</h3><p>热更新</p>
]]></content>
  </entry>
  <entry>
    <title>javascript闭包</title>
    <url>/2021/03/01/js%E9%97%AD%E5%8C%85/</url>
    <content><![CDATA[<h2 id="JS-中的闭包"><a href="#JS-中的闭包" class="headerlink" title="JS 中的闭包"></a>JS 中的闭包</h2><p>下面定义了一个工厂函数 <code>createIncrement(i)</code>，它返回一个<code>increment</code>函数。之后，每次调用<code>increment</code>函数时，内部计数器的值都会增加<code>i</code>。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">function createIncrement(i) &#123;</span><br><span class="line">  let value = 0;</span><br><span class="line">  function increment() &#123;</span><br><span class="line">    value += i;</span><br><span class="line">    console.log(value);</span><br><span class="line">  &#125;</span><br><span class="line">  return increment;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">const inc = createIncrement(1);</span><br><span class="line">inc(); // 1</span><br><span class="line">inc(); // 2</span><br></pre></td></tr></table></figure>
<p><code>createIncrement(1)</code> 返回一个增量函数，该函数赋值给<code>inc</code>变量。当调用<code>inc()</code>时，<code>value</code> 变量加<code>1</code>。</p>
<p>第一次调用<code>inc()</code>返回<code>1</code>，第二次调用返回<code>2</code>，依此类推。</p>
<p>这挺趣的，只要调用<code>inc()</code>还不带参数，JS 仍然知道当前 <code>value</code> 和 <code>i</code> 的增量，来看看这玩意是如何工作的。</p>
<p>原理就在 <code>createIncrement()</code> 中。当在函数上返回一个函数时，有会有闭包产生。闭包捕获词法作用域中的变量 <code>value</code> 和 <code>i</code>。</p>
<p>词法作用域是定义闭包的外部作用域。在本例中，<code>increment()</code> 的词法作用域是<code>createIncrement()</code>的作用域，其中包含变量 <code>value</code> 和 <code>i</code>。</p>
]]></content>
      <categories>
        <category>js</category>
      </categories>
      <tags>
        <tag>js</tag>
      </tags>
  </entry>
</search>
